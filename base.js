console.log('starting in base');
// let { projectName, projectId } = require('./modules');
let { projectId } = require('./modules');
console.log(`has id: ${projectId}`);
console.log(`ending in base`);